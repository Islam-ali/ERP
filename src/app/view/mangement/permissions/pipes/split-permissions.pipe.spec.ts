import { SplitPermissionsPipe } from './split-permissions.pipe';

describe('SplitPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
